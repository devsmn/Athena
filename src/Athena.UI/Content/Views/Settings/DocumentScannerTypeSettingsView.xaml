<?xml version="1.0" encoding="utf-8" ?>
<ui:ContextContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    Style="{StaticResource ContentPageStyle}"
                    xmlns:loc="clr-namespace:Athena.Resources.Localization"
                    xmlns:sfb="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
                    xmlns:ui="clr-namespace:Athena.UI"
                    x:Class="Athena.UI.DocumentScannerTypeSettingsView"
                    Title="DocumentScannerTypeSettings">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label
            Style="{x:StaticResource HeaderLabel}"
            Text="{x:Static loc:Localization.DocumentScanner}" />

        <Label
            Text="{x:Static loc:Localization.DocumentScannerDesc}"
            Grid.Row="1"
            Margin="10,10,10,10" />

        <Label
            Grid.Row="2"
            Text="{x:Static loc:Localization.DocumentScannerSettingsHint}"
            IsVisible="{Binding ShowSettingsHint}"
            Margin="10,10,10,10" />

        <sfb:SfCheckBox
            Grid.Row="3"
            IsChecked="{Binding UseAdvancedScanner}"
            Margin="5"
            Text="{x:Static loc:Localization.DocumentScannerUseAdv}" />

        <sfb:SfButton
            Grid.Row="4"
            Margin="5,5,5,20"
            Command="{Binding CloseCommand}"
            VerticalOptions="End"
            Text="{x:Static loc:Localization.Save}"
            HorizontalOptions="Center"
            Style="{x:StaticResource PrimaryButtonFlat}"/>

    </Grid>
</ui:ContextContentPage>
