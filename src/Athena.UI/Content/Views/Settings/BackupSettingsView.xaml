<?xml version="1.0" encoding="utf-8" ?>
<ui:DefaultContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:p="clr-namespace:Syncfusion.Maui.Popup;assembly=Syncfusion.Maui.Popup"
                    x:Class="Athena.UI.BackupSettingsView"
                    xmlns:loc="clr-namespace:Athena.Resources.Localization"
                    xmlns:sfb="clr-namespace:Syncfusion.Maui.Buttons;assembly=Syncfusion.Maui.Buttons"
                    xmlns:ui="clr-namespace:Athena.UI"
                    Title="BackupSettingsView">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <HorizontalStackLayout Grid.Row="0" VerticalOptions="Center" HorizontalOptions="Center" Grid.ColumnSpan="2">
            <Label Text="{x:Static loc:Localization.BackupTitle}"
                   Margin="5,20,5,2"
                   FontAttributes="Bold"
                   HorizontalTextAlignment="Center"
                   FontSize="32"/>

            <sfb:SfButton Margin="3,20,0,2"
                          Clicked="OnInfoClicked"
                          HorizontalTextAlignment="Start"
                          BackgroundImageSource="info.png"
                          ImageSource="info.png"
                          Background="Transparent"
                          HeightRequest="25"
                          WidthRequest="25"
                          VerticalTextAlignment="Center"
                          HorizontalOptions="Center"
                          ImageAlignment="Default"
                          StrokeThickness="0"/>
        </HorizontalStackLayout>

        <p:SfPopup x:Name="sfPopup"
                   HeaderTitle="Loading"
                   AutoSizeMode="Height"
                   Grid.Row="0"
                   Grid.RowSpan="3"
                   StaysOpen="True"
                   IsOpen="{Binding IsBusy}">
            <p:SfPopup.ContentTemplate>
                <DataTemplate >
                    <VerticalStackLayout HorizontalOptions="Center">
                        <ActivityIndicator IsRunning="true" />
                        <Label Text="{Binding BusyText}"
                               Margin="10"
                               VerticalTextAlignment="Center"
                               HorizontalTextAlignment="Center"/>
                    </VerticalStackLayout>
                </DataTemplate>
            </p:SfPopup.ContentTemplate>
        </p:SfPopup>

        <sfb:SfButton
            Grid.Row="1"
            Margin="5"
            Text="{x:Static loc:Localization.Create}"
            VerticalOptions="End"
            Command="{Binding CreateBackupCommand}"
            Style="{x:StaticResource ListButton}"/>

        <sfb:SfButton
            Text="{x:Static loc:Localization.Restore}"
            Grid.Row="2"
            Margin="5"
            Command="{Binding RestoreBackupCommand}"
            Style="{x:StaticResource ListButton}"/>
    </Grid>
</ui:DefaultContentPage>
